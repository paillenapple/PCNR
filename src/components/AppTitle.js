import React from 'react';
import styled from 'styled-components';

const StyledAppTitle = styled.h1`
  text-align: center;
`

const AppTitle = () => (
  <StyledAppTitle>piou-piou</StyledAppTitle>
)

export default AppTitle;